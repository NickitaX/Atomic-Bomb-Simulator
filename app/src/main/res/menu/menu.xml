<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_facebook"
        android:icon="@drawable/ic_share_white_24dp"
        android:title="Facebook"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_feedback"
        android:icon="@drawable/ic_mood_white_24dp"
        android:title="Feedback"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_faq"
        android:icon="@drawable/ic_error_white_24dp"
        android:title="Alert"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_request_gps"
        android:icon="@drawable/ic_gps_fixed_white_24dp"
        android:title="Request GPS"
        app:showAsAction="always"/>

</menu>